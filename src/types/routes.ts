enum PublicRoutes {
  login = '/login',
  register = '/register',
}

enum PrivateRoutes {
  root = '/',
  pacientes = '/pacientes',
  citas = '/citas',
  finanzas = '/finanzas',
  mensajes = '/mensajes',
  noticias = '/noticias',
}

export { PublicRoutes, PrivateRoutes }
